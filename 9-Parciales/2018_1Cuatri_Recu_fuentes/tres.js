function mostrar()
{
    var precio;
    var cantidad
    var porsentaje;
    var resultado;

    precio = parseFloat(parseInt(prompt"ingrese cantidad"));
    cantidad = paseint(prompt"cantidad de personas")
    
    alert("Su cuenta en total es de " + resultado + " no incluye iva");

}
